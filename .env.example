DATABASE_URL="postgresql://user:password@localhost:5432/voyage_ivana"

# Optional: only needed on Neon (e.g. Vercel's Postgres/Neon integration sets
# this automatically). Prisma CLI commands (migrate deploy, etc.) use this
# direct connection instead of the pooled DATABASE_URL — see prisma.config.ts.
# DATABASE_URL_UNPOOLED="postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/voyage_ivana?sslmode=require"
